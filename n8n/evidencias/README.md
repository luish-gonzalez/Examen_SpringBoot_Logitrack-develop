# Evidencias pendientes de n8n

Este directorio está reservado para capturas reales obtenidas posteriormente
del workflow **Resumen diario de inventario**:

- `ejecucion-exitosa`: ejecución real con salida `EXITO`.
- `error-controlado`: ejecución real con salida `ERROR`, provocada de forma
  segura al detener temporalmente el MCP o usar temporalmente una credencial
  Bearer incorrecta.

No hay evidencias completadas ni capturas inventadas en esta etapa.
